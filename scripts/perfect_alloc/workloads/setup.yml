load:
  num_records: 6000000
  distribution:
    type: uniform
    range_min: 0
    range_max: 10000000

# Dummy workload. We only use this config for the dataset.
run:
- num_requests: 1
  read:
    proportion_pct: 100
    distribution:
      type: uniform
