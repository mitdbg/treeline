load:
  num_records: 1000000
  distribution:
    type: uniform
    range_min: 1
    range_max: 10000000

# Dummy workload. We only use this config for the dataset.
run:
- num_requests: 1
  read:
    proportion_pct: 100
    distribution:
      type: uniform
