JOBS = [
  "psync-4k",
  "psync-16k",
  "psync-64k",
  "uring-4k",
  "uring-16k",
  "uring-64k",
]

run_experiment_group(
  name="write",
  run="../run_fio.sh",
  experiments=[
    ExperimentInstance(
      name="write-{}".format(jobfile),
      args=[jobfile],
    )
    for jobfile in JOBS
  ],
)
