JOBS = [
  "psync-4k",
  "psync-16k",
  "psync-64k",
  "uring-4k",
  "uring-16k",
  "uring-64k",
]

run_experiment_group(
  name="read",
  run="../run_fio.sh",
  experiments=[
    ExperimentInstance(
      name="read-{}".format(jobfile),
      args=[jobfile],
    )
    for jobfile in JOBS
  ],
)
