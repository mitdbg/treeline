#include "util/insert_tracker.h"

#include <algorithm>
#include <optional>
#include <random>
#include <string>
#include <utility>
#include <vector>

#include "gtest/gtest.h"
#include "llsm/slice.h"

namespace {

using llsm::InsertTracker;
using llsm::Slice;

TEST(InsertTrackerTest, Hello) {
  ASSERT_TRUE(true);
}

}  // namespace
